document.addEventListener('DOMContentLoaded',function(){const checks=document.querySelectorAll('.employee-check'),label=document.getElementById('employee-picker-label'),inputs=document.getElementById('selected-employee-inputs');function syncEmployees(){if(!inputs||!label)return;inputs.innerHTML='';const selected=[];checks.forEach(function(check){if(check.checked){selected.push(check.dataset.name);const input=document.createElement('input');input.type='hidden';input.name='assigned_to';input.value=check.value;inputs.appendChild(input);}});label.innerHTML=selected.length?'<i class="bi bi-people me-1"></i> '+selected.length+' employee'+(selected.length===1?'':'s')+' selected':'<i class="bi bi-plus-circle me-1"></i> Choose employees';}checks.forEach(function(check){check.addEventListener('change',syncEmployees);});});